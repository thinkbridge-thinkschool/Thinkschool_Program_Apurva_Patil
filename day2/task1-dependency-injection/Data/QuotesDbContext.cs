namespace QuotesApi.Data;

// Note: For the exercise we keep a lightweight stub. In a real app this would
// derive from Microsoft.EntityFrameworkCore.DbContext and be registered with
// AddDbContext<T>() which also results in a scoped lifetime.
public class QuotesDbContext
{
}
