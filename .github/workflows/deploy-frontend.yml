name: Deploy Frontend to Azure Static Web Apps

on:
  push:
    branches:
      - master
    paths:
      - 'week3/Day17/Task1/quotes-frontend/**'
  workflow_dispatch:

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    name: Build and Deploy
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: week3/Day17/Task1/quotes-frontend/package-lock.json

      - name: Install dependencies
        working-directory: week3/Day17/Task1/quotes-frontend
        run: npm ci

      - name: Build (production)
        working-directory: week3/Day17/Task1/quotes-frontend
        run: npm run build

      - name: Deploy to Azure Static Web Apps
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: upload
          app_location: week3/Day17/Task1/quotes-frontend
          output_location: dist/quotes-frontend/browser
          skip_app_build: true
